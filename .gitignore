###############################################################################
#  📦  Workspace / Yarn                                                        #
###############################################################################
# Top-level node_modules (created by yarn node-modules linker)
node_modules/

# Any nested node_modules that might sneak in
**/node_modules/

# Yarn 4 offline cache zips – big and reproducible, so keep them out
.yarn/cache/

# Ignore everything else in .yarn/* …
.yarn/*
# … but *keep* the things that belong in the repo:
!.yarn/patches/
!.yarn/plugins/
!.yarn/releases/
!.yarn/sdks/
!.yarn/versions/

# Plug'n'Play manifest (only present if you switch back to PnP)
.pnp.*

###############################################################################
#  🚀  Build / Runtime Artifacts                                               #
###############################################################################
# Next.js build output
apps/web/.next/

# NestJS build output
apps/api/dist/

# Turborepo incremental cache
.turbo/

# Coverage reports
coverage/

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

###############################################################################
#  🌐  Environment & local config                                             #
###############################################################################
# dotenv files (commit templates separately if you like)
.env
.env.*            # .env.local, .env.production, etc.

# VS Code settings generated by yarn sdks
.vscode/*         # keep tasks or snippets by adding ! overrides below
!.vscode/README.md

# JetBrains IDEs
.idea/

# macOS / Windows noise
.DS_Store
Thumbs.db

###############################################################################
#  🐳  Docker                                                                  #
###############################################################################
# Named volume data (only if you accidentally create them locally)
db_data/
